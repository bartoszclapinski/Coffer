<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Coffer.Application.ViewModels.Transactions;assembly=Coffer.Application"
             xmlns:conv="clr-namespace:Coffer.Desktop.Converters"
             mc:Ignorable="d" d:DesignWidth="760" d:DesignHeight="640"
             x:Class="Coffer.Desktop.Views.TransactionsView"
             x:DataType="vm:TransactionsViewModel"
             x:CompileBindings="False">
    <Grid Margin="40,32" RowDefinitions="Auto,Auto,*">
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
            <StackPanel Grid.Column="0" Spacing="4">
                <TextBlock Text="Transakcje"
                           FontSize="26"
                           FontWeight="Light"
                           Foreground="#1D1D1F" />
                <TextBlock Text="Lista transakcji z ostatnich miesięcy."
                           Foreground="#8E8E93" />
            </StackPanel>
            <Button Grid.Column="1"
                    VerticalAlignment="Center"
                    Content="Kategoryzuj istniejące"
                    Command="{Binding RecategorizeExistingCommand}" />
        </Grid>

        <!-- Filter bar -->
        <Grid Grid.Row="1" Margin="0,20,0,0" ColumnDefinitions="*,12,170,12,170,12,150">
            <TextBox Grid.Column="0"
                     Text="{Binding SearchText, Mode=TwoWay}"
                     Watermark="Szukaj po opisie lub sprzedawcy…" />
            <ComboBox Grid.Column="2"
                      HorizontalAlignment="Stretch"
                      PlaceholderText="Wszystkie konta"
                      ItemsSource="{Binding Accounts}"
                      SelectedItem="{Binding SelectedAccount, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{Binding Name}" />
                            <TextBlock Text="{Binding BankCode}" Foreground="#8E8E93" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <ComboBox Grid.Column="4"
                      HorizontalAlignment="Stretch"
                      PlaceholderText="Wszystkie kategorie"
                      ItemsSource="{Binding CategoryFilters}"
                      SelectedItem="{Binding SelectedCategoryFilter, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Ellipse Width="10" Height="10" VerticalAlignment="Center"
                                     Fill="{Binding Color, Converter={x:Static conv:HexColorToBrushConverter.Instance}}" />
                            <TextBlock Text="{Binding Name}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <ComboBox Grid.Column="6"
                      HorizontalAlignment="Stretch"
                      ItemsSource="{Binding DateRanges}"
                      SelectedItem="{Binding SelectedRange, Mode=TwoWay}" />
        </Grid>

        <Panel Grid.Row="2" Margin="0,20,0,0">
            <ProgressBar IsIndeterminate="True" Height="4"
                         VerticalAlignment="Top"
                         IsVisible="{Binding IsLoading}" />

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#D14"
                       TextWrapping="Wrap"
                       VerticalAlignment="Top"
                       IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />

            <TextBlock Text="Brak transakcji. Zmień filtry lub zaimportuj wyciąg, aby je tu zobaczyć."
                       Foreground="#8E8E93"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       IsVisible="{Binding IsEmpty}" />

            <DataGrid ItemsSource="{Binding Transactions}"
                      IsVisible="{Binding !IsEmpty}"
                      IsReadOnly="True"
                      CanUserResizeColumns="True"
                      CanUserReorderColumns="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="#FFFFFF"
                      BorderBrush="#E5E5EA"
                      BorderThickness="1"
                      CornerRadius="8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Data"
                                        Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}"
                                        Width="110" />
                    <DataGridTextColumn Header="Opis"
                                        Binding="{Binding Description}"
                                        Width="2*" />
                    <DataGridTextColumn Header="Sprzedawca"
                                        Binding="{Binding Merchant}"
                                        Width="*" />
                    <DataGridTextColumn Header="Konto"
                                        Binding="{Binding AccountName}"
                                        Width="*" />
                    <DataGridTemplateColumn Header="Kategoria" Width="1.6*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="Auto,*" VerticalAlignment="Center" Margin="6,0">
                                    <Ellipse Grid.Column="0" Width="10" Height="10" Margin="0,0,8,0"
                                             VerticalAlignment="Center"
                                             IsVisible="{Binding SelectedCategory, Converter={x:Static ObjectConverters.IsNotNull}}"
                                             Fill="{Binding SelectedCategory.Color, Converter={x:Static conv:HexColorToBrushConverter.Instance}}" />
                                    <ComboBox Grid.Column="1"
                                              HorizontalAlignment="Stretch"
                                              PlaceholderText="Bez kategorii"
                                              ItemsSource="{Binding Categories}"
                                              SelectedItem="{Binding SelectedCategory, Mode=TwoWay}">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Spacing="6">
                                                    <Ellipse Width="10" Height="10" VerticalAlignment="Center"
                                                             Fill="{Binding Color, Converter={x:Static conv:HexColorToBrushConverter.Instance}}" />
                                                    <TextBlock Text="{Binding Name}" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Kwota" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Right"
                                           Margin="0,0,12,0"
                                           VerticalAlignment="Center"
                                           FontWeight="SemiBold"
                                           Foreground="{Binding Amount, Converter={x:Static conv:AmountToBrushConverter.Instance}}">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0:N2} {1}">
                                            <Binding Path="Amount" />
                                            <Binding Path="Currency" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Panel>
    </Grid>
</UserControl>
