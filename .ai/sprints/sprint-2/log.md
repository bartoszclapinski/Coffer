# Log sprintu 2

## 2026-05-13

- kroki 2.1-2.2 ukończone — `System.Security.Cryptography.ProtectedData` (`9.*`) dodany do `Coffer.Infrastructure.csproj`, `Xunit.SkippableFact` (`1.*`) do `Coffer.Infrastructure.Tests.csproj`
- kroki 2.3-2.4 ukończone — `Coffer.Core/Security/IKeyVault.cs` z 3 async metodami, namespace `Coffer.Core.Security`. Czysty BCL: tylko `Task`, `byte[]`, `TimeSpan`, `CancellationToken`. Hard rule #3 utrzymany — Core ma `Microsoft.Extensions.DependencyInjection.Abstractions` (z Sprintu 1) ale nic crypto/platform-specific.
- krok 2.5 ukończony — `CofferPaths.cs` z `LocalAppDataFolder()` i `MasterKeyCacheFile()`. Cross-platform przez `Environment.SpecialFolder.LocalApplicationData`.
- krok 2.6 ukończony — `WindowsDpapiKeyVault.cs` z `[SupportedOSPlatform("windows")]`, dual constructor (parametryczny + bezparametrowy z default path), format pliku `[expiresAtUtcTicks: 8B][keyLength: 4B][masterKey: N B]` w `BuildPayload/TryParsePayload`, memory hygiene przez `Array.Clear` w try-finally, expired cache auto-delete, swallowed `IOException`/`UnauthorizedAccessException` na delete (best-effort)
- krok 2.7 ukończony — `InMemoryKeyVault.cs` cross-platform fallback, thread-safe (lock), zwraca clone cache key
- krok 2.8 ukończony — DI: `AddCofferKeyVault` w `Coffer.Infrastructure/DependencyInjection/ServiceRegistration.cs` z `OperatingSystem.IsWindows()` jako platform guard. Non-Windows wybiera `InMemoryKeyVault` z `LogInformation` o tym wyborze (poprzez DI-resolved `ILogger<InMemoryKeyVault>`)
- kroki 2.9-2.10 ukończone — 5 testów `InMemoryKeyVaultTests` (cross-platform), 7 testów `WindowsDpapiKeyVaultTests` (z `SkippableFact` + `Skip.IfNot(OperatingSystem.IsWindows(), ...)`). Test isolation via `Path.Combine(Path.GetTempPath(), Guid.NewGuid().ToString("N"))` + `IDisposable.Dispose` cleanup.
- problem: build failowal z `CA1062: Validate arguments of public methods` na `SetCachedMasterKeyAsync(byte[] masterKey, ...)` w obu vault'ach. Per [conventions.md](../../../docs/conventions.md) zgodnie z rekomendacją: → rozwiązanie: dodano `ArgumentNullException.ThrowIfNull(masterKey)` na początku każdej `Set` metody.
- problem: build failowal z 11x `CA1416: This call site is reachable on all platforms; method only supported on 'windows'` w `WindowsDpapiKeyVaultTests` mimo `Skip.IfNot(OperatingSystem.IsWindows(), ...)`. Analyzer nie rozpoznaje `Skip.IfNot` jako platform guard (rozpoznaje tylko `if (OperatingSystem.IsWindows())`) → rozwiązanie: dodano `[SupportedOSPlatform("windows")]` na klasie testowej. Klasa kompiluje się cross-platform mimo atrybutu (tylko hint dla analyzera); xUnit runner na Ubuntu CI uruchamia testy które `Skip.IfNot` zatrzymuje runtime'em.
- krok 2.11 ukończony — `dotnet build` (0 warnings, 0 errors), `dotnet test` 18 testów pass (1 Core SmokeTest + 3 Application ServiceRegistrationTests + 14 Infrastructure: 5 InMemory + 7 DPAPI + 2 Serilog), `dotnet format --verify-no-changes` zielono
- problem: post-checkout autocrlf w pliku `tests/Coffer.Application.Tests/DependencyInjection/ServiceRegistrationTests.cs` ze Sprintu 1 zmienił LF na CRLF, `dotnet format --verify` failowal. → rozwiązanie: `dotnet format` (bez --verify) naprawił, plik zostanie w nowej formie w tym commicie (drobny diff ze Sprintu 1)
- decyzja: krok 2.12 (manual cross-process DPAPI verification) **pominięty** — testy 2.10 weryfikują round-trip plik→DPAPI→plik→ProtectedData.Unprotect w jednym procesie, co wystarcza by udowodnić że DPAPI persistencja działa. Cross-process persistencja dla tego samego usera to gwarancja Windows OS (nie naszego kodu); manualny test nie dodał by nowej informacji
