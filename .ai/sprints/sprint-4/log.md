# Sprint 4 log

## 2026-05-16

- Issue #20 created for the implementation (`feat(sprint-4): CofferDbContext + SqlCipherKeyInterceptor + InitialCreate migration`), labels `feat` + `sprint-4`. Workflow from issue #10 applied.
- Steps 4.1-4.3 done — added `Microsoft.EntityFrameworkCore` (`9.*`), `.Sqlite` (`9.*`), `.Design` (`9.*`, `PrivateAssets=all`), `SQLitePCLRaw.bundle_e_sqlcipher` (`2.*`) to Coffer.Infrastructure. Tool manifest `.config/dotnet-tools.json` created via `dotnet new tool-manifest`; `dotnet-ef 9.0.16` installed locally.
- Steps 4.4-4.7 done — `SchemaInfoEntry` (Id/Version/MigratedAt/AppVersion), `CofferDbContext` (table `_SchemaInfo`, unique index on `Version`, global `decimal(18,2)` convention), `SqlCipherKeyInterceptor` (`PRAGMA key = "x'..'"` via `Convert.ToHexString`), `CofferPaths.DatabaseFile()` returning `%LocalAppData%/Coffer/coffer.db`.
- Step 4.8 done — `CofferDbContextDesignFactory : IDesignTimeDbContextFactory<CofferDbContext>` using `Data Source=:memory:` (no SQLCipher needed for `dotnet ef migrations add`).
- Step 4.9 done — `dotnet ef migrations add InitialCreate --project src/Coffer.Infrastructure --output-dir Persistence/Migrations` produced `20260516142523_InitialCreate.cs` + `.Designer.cs` + `CofferDbContextModelSnapshot.cs`.
- Problem: generated `InitialCreate.cs` triggered `CA1062` (no `ArgumentNullException.ThrowIfNull(migrationBuilder)` in `Up`/`Down`) which `TreatWarningsAsErrors=true` promoted to error. → Resolution: added `src/Coffer.Infrastructure/Persistence/Migrations/.editorconfig` suppressing `CA1062` for the folder only. Production code outside the Migrations folder keeps the guard.
- Steps 4.10-4.11 done — `MigrationResult(MigrationStatus Status, IReadOnlyList<string> AppliedMigrations)` record + `MigrationStatus { UpToDate, Migrated }` enum + static helpers. `MigrationRunner` with optional `Func<CancellationToken, Task>? preMigrationBackup` constructor parameter, `RunPendingMigrationsAsync` calls callback before `MigrateAsync` and appends `SchemaInfoEntry` after.
- Steps 4.12-4.13 done — `AddCofferDatabase(IServiceCollection, Func<IServiceProvider, byte[]> dekProvider)` extension registers `IDbContextFactory<CofferDbContext>` and `MigrationRunner` as Transient. Not invoked from `AddCofferInfrastructure` automatically — Sprint 5 setup wizard will call it explicitly.
- Steps 4.14-4.15 done — 7 integration tests: 3 in `CofferDbContextEncryptionTests` (round-trip with same DEK, throw with different DEK, raw bytes don't contain plaintext sentinel) + 4 in `MigrationRunnerTests` (callback order, no callback when up-to-date, `_SchemaInfo` appended, result content).
- Problem: first test run had 3 fail / 4 pass — `Read_WithDifferentDek_Throws` worked but `WriteThenRead_WithSameDek` failed on reopen with `SQLite Error 26: 'file is not a database'`. Hypothesis (sync vs async interceptor): EF Core 9's async migration paths (`MigrateAsync`, `AcquireDatabaseLockAsync`) only call `ConnectionOpenedAsync`, not the sync `ConnectionOpened` I had overridden. → Resolution: override both sync `ConnectionOpened` and async `ConnectionOpenedAsync` in `SqlCipherKeyInterceptor` (delegate to a shared private helper). All 7 Persistence tests pass after the fix.
- Decision: `Pooling=False` kept in test connection strings as defensive isolation — guarantees a fresh underlying connection per `DbContext` so test setup state is deterministic. Production `AddCofferDatabase` uses default pooling; the async-interceptor fix makes that safe regardless of pool reuse.
- Problem: `dotnet format --verify-no-changes` flagged the auto-generated migration with `CHARSET` (UTF-8 BOM) and `IDE0161` (file-scoped namespace preference) — both come from EF Core's generator. → Resolution: extended `Migrations/.editorconfig` with `charset = utf-8-bom`, `dotnet_diagnostic.IDE0161.severity = none`. Format scope outside the Migrations folder is unchanged.
- Problem: `tests/Coffer.Infrastructure.Tests/Security/DekFileTests.cs` (Sprint 3) flagged CRLF/LF mismatches — known autocrlf side effect after branch checkout. → Resolution: `dotnet format` (no `--verify`) normalised it.
- Step 4.16 done — `dotnet build` (0 warnings, 0 errors), `dotnet test` 47 pass total (1 Core + 3 Application + 43 Infrastructure: 5 InMemory + 7 DPAPI + 2 Serilog + 4 Argon2 + 6 BIP39 + 4 AES-GCM + 4 DekFile + 3 Encryption + 4 MigrationRunner), `dotnet format --verify-no-changes` green.
- Code review on PR #21 raised 12 points. Eight addressed in a follow-up commit on the same branch; four (multi-entry `_SchemaInfo`, DEK hex memory hygiene, `MaxLength` constraints, `IDisposable` on the interceptor) deferred to a separate chore PR after the sprint closure, per the agreed scope.
  - **#1 `MigrationResult` accuracy**: `RunPendingMigrationsAsync` now snapshots `GetAppliedMigrationsAsync` before and after `MigrateAsync`, returning the actual diff. Added `MigrationStatus.Failed` with `MigrationResult.Failed(appliedBeforeFailure)`. `MigrateAsync` is wrapped in a broad `catch` that re-throws `OperationCanceledException` and converts every other exception into a `Failed` result with the partial-applied list logged. Backup-callback failure is unchanged (propagates) — hard rule #8.
  - **#3 Sync-over-async interceptor**: `SqlCipherKeyInterceptor.ConnectionOpenedAsync` now uses `cmd.ExecuteNonQueryAsync(cancellationToken)` directly. Extracted `BuildPragmaKeyCommand()` to share the command-text generation between sync and async paths.
  - **#5 `dekProvider` lifecycle**: added a detailed XML-doc `<param>` block on `AddCofferDatabase` explaining one-shot invocation and immutable-DEK-for-container-lifetime semantics. No code change.
  - **#8 Specific SqliteErrorCode**: `Read_WithDifferentDek_Throws` now asserts `SqliteErrorCode == 26` (`SQLITE_NOTADB`) — locks the test to the SQLCipher failure mode instead of catching any `SqliteException`.
  - **#9 Backup-throws regression test**: new `Run_WhenBackupCallbackThrows_DoesNotApplyAnyMigration` test — fires a throwing callback, asserts the exception propagates and `GetAppliedMigrationsAsync` is still empty afterwards. Hard rule #8 covered.
  - **#10 Stronger ordering**: rewrote `Run_WithPendingMigrations_InvokesBackupCallbackBeforeMigrate` to verify state *inside* the callback — `GetPendingMigrationsAsync()` non-empty AND `GetAppliedMigrationsAsync()` empty. "Before MigrateAsync" is now empirically proven.
  - **#11 Specific Version**: `Run_AfterSuccessfulMigration_AppendsSchemaInfoEntryWithExactVersion` checks `Version == "20260516142523_InitialCreate"`. Also tightened `Run_ReturnsMigratedResult_WithAppliedMigrationNames` to `ContainSingle().Which.Should().Be(...)`.
  - **#12 `appVersionProvider` injection**: `MigrationRunner` constructor takes an optional `Func<string>? appVersionProvider` defaulting to the entry-assembly resolver. New test `Run_RecordsAppVersionFromInjectedProvider` verifies `_SchemaInfo.AppVersion == "1.2.3-test"`.
  - Documented memory-hygiene caveat for `Convert.ToHexString(_dek)` directly in `SqlCipherKeyInterceptor` XML-doc remarks — primes the follow-up sprint that will switch to a zeroable `Span<char>` path.
- Step 4.16 (round 2) done — `dotnet build` (0/0), `dotnet test` 49 pass total (+2 new: `Run_WhenBackupCallbackThrows_…` and `Run_RecordsAppVersionFromInjectedProvider`; rewritten ordering and version-exact tests), `dotnet format --verify-no-changes` green.
- Naming nit: const `ExpectedInitialMigration` flagged `IDE1006` (private fields need `_camelCase` per `.editorconfig`); renamed to `_expectedInitialMigration` matching the project pattern established in Sprint 1 and Sprint 3.
- Step 4.18 done — commits `69aecf6` (initial implementation) + `16e7585` (code-review fixes) pushed to `feature/sprint-4-dbcontext-sqlcipher`, PR #21 opened with `Closes #20`, labels `feat` + `sprint-4`.
- Step 4.19 done — CI green on the final commit: `build-and-test` 34s, `format-check` 41s. PR #21 squash-merged as `d86d17d`; branch deleted. Issue #20 auto-closed via `Closes #20`.
- Problem (recurrence): post-merge `git pull` again hit `unable to unlink: Invalid argument` on csproj files despite `dotnet build-server shutdown` and `Get-Process dotnet, MSBuild, vbcscompiler | Stop-Process`. Some Roslyn-flavoured child process apparently respawns or holds handles independently. → Resolution: re-killed the process group, then `git reset --hard HEAD` + `git clean -fd` + `git pull` cleaned up (all local working-tree state was already in the squash-merged PR). Recurring lesson: even with the `vbcscompiler` kill from Sprint 3, Windows file handles can linger one more cycle. Watch for it on future merges; consider a `git config --global core.fscache true` or similar follow-up.
- Step 4.20 — closure issue #22, branch `chore/close-sprint-4`, sprint closed: status in `sprint-4.md` set to `Closed`, `index.md` updated.
- Sprint closed 2026-05-17.
