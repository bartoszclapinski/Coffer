# Sprint 19 log

## 2026-07-02

- `--:--` sprint planned — spending explorer: selectable-window category breakdown + merchant drill-down. Beyond-roadmap; deepens the Phase-6 dashboard from fixed overview into an interactive analysis surface, and finally surfaces `Transaction.Merchant` as a spending dimension. No migration (data already imported), no AI (pure read-side SQL). Two implementation PRs planned: 19-A (`ISpendingExplorerQuery` + `SpendingExplorerViewModel`, headless) → 19-B (Avalonia "Wydatki / Spending" page + nav + i18n).
- `--:--` scope decision: **core only** for this sprint — selectable window + category→merchant→transaction drill-down. Period-over-period delta, a `GetSpendingByMerchant` chat tool, and charts are explicitly deferred to keep the PRs digestible.
- `--:--` 19-A complete (steps 19.1–19.5) — headless read-side. `ISpendingExplorerQuery` + three result records (`SpendingWindow`, `CategorySpend`, `MerchantSpend`) in `Coffer.Core/Spending/`; `SpendingExplorerQuery` in `Coffer.Infrastructure/Spending/` (server-side `GROUP BY`/`AsNoTracking`, debits as positive PLN magnitudes, inclusive window, per-optional-account; null/blank merchants collapse into one unknown bucket keyed null; a drill's null category = the uncategorised bucket, not "all"). `SpendingExplorerViewModel` + `SpendingWindowResolver` in `Coffer.Application/ViewModels/Spending/` (window presets + custom range, optional account, category→merchant→transaction drill with a breadcrumb, money via `CashFlowDisplay`, uncategorised/unknown labels localised at the VM boundary). Registered in Infrastructure `AddCofferImport` + Desktop DI. Tests: 6 Infrastructure (real SQLCipher) + 13 Application (VM over fakes + pure resolver). Full suite green (573: 26 Core + 147 Application + 400 Infrastructure). No migration, no AI. UI + i18n follow in 19-B.
- `--:--` decision: preset labels/fallbacks reference i18n keys from the VM but the `.resx` keys are added in 19-B (per the plan's step split), so 19-A stays headless; VM tests assert the echoed key via `FakeLocalizer`.
- `--:--` problem: seeding transactions after `SeedAccountsAsync` tripped a `UNIQUE constraint failed: Accounts.Id` (the `NewTransaction` navigation re-inserts the account) → resolution: drop the separate account seed and let the transaction's `Account` navigation insert it once, matching the existing `VariableBurnQueryTests` idiom.
- `--:--` problem: 19-A `format-check` failed on CI (Linux) with `IDE1006` — the static readonly test fixture `WholeYear` needs the private-static-readonly `_` prefix → fixed to `_wholeYear` (PR #164, follow-up commit). Local `dotnet format` scan (filtering the known CRLF/ENDOFLINE noise) now surfaces such style errors before pushing.
- `--:--` 19-B complete (steps 19.6–19.10) — the UI. A dedicated **Wydatki / Spending** Avalonia page (`SpendingExplorerView.axaml` + `.axaml.cs`): account + period selectors, custom-range date pickers (shown only for the custom preset), a breadcrumb (`window › category › merchant`) with Back + the level total, and three swappable level cards — categories and merchants as drill buttons with a share `ProgressBar`, the transaction leaf as a read-only list. Nav wired in `MainViewModel` (`Spending` page + `IsSpendingActive` + `ShowSpending`, ctor param between affordability and settings) and `MainWindow.axaml` (VM namespace + DataTemplate + sidebar button after Transactions). Added a small `IsEmpty` flag to the VM for the per-level empty state. 26 i18n keys added to both `.resx` (Nav.Spending, Spending.* window/section/label/fallback/error), parity green. Tests: `ShowSpending_SwitchesActivePage` + the existing parity test. Full suite green (574: 26 Core + 148 Application + 400 Infrastructure).
- `--:--` 19.11 (manual DoD click-through) deferred to manual — needs a running desktop app + real imported data, like the other sprints' manual steps.
