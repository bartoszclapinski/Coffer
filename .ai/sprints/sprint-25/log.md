# Sprint 25 log

## 2026-07-05

- `--:--` sprint planned — restore from a BIP39 seed (doc 08 "Restore from seed" + doc 09 dual-wrap key hierarchy). Plan written to `sprint-25.md`.
- `--:--` decision: **DEK dual-wrap via `DekFile` v2, backward-compatible with v1** — v2 appends a seed-wrapped `{iv, tag, ciphertext}` of the DEK after the existing password blob; `ReadAsync` accepts `version is 1 or 2` (seed fields null for v1); `CurrentVersion` → 2. A v1 file is never auto-upgraded on read (upgrading needs the DEK in hand — see Enable).
- `--:--` decision: **seed derivation passphrase = fixed `"Coffer"`** (doc 09's `DeriveSeed("Coffer")`) — domain separation, frozen once a v2 vault exists.
- `--:--` decision: **DEK stays wrapped by both channels, derived from neither** — restore-from-seed decrypts the DEK via the seed blob then re-wraps under a *new* master password (fresh Argon2 salt), rewriting the same seed blob; only the ~60-byte DEK file changes, never the database.
- `--:--` decision (key UX): **"Enable seed recovery" mints a NEW functional seed, does not trust the old paper one** — v1 setup showed a seed but wrapped nothing with it, so the owner's original paper seed is cryptographically inert. Enable generates/displays/verifies a fresh mnemonic (like setup) and wraps the in-holder DEK with it. Consequence: the original paper seed becomes irrelevant; the newly shown seed is the real recovery seed. Flagged as the top open question for the owner.
- `--:--` decision: **recovery entry lives on the login window** (existing-vault, forgot-password) — the realistic scenario routes to login, not setup; a v1 vault shows "predates seed recovery, use your password then enable it in Settings". Fresh-install/foreign-machine/archive restore deferred (needs archive import).
- `--:--` note: all primitives exist (Sprint 3) — `Bip39SeedManager.DeriveRecoveryKeyAsync` already returns a 32-byte recovery key; the DEK is just AES-GCM-wrapped with it. The gap is purely wiring (`SetupService` ignores the mnemonic today) + the v2 file format. See [[project_dek-format-seed-gap]].
- `--:--` structure: 25-A DEK dual-wrap + `ISeedRecoveryService` (headless) → 25-B restore-from-seed UI (login window) → 25-C enable-seed-recovery in Settings.
- `--:--` 25-A complete — `DekFile` v2 (dual-wrap, back-compat v1 read, `WriteReplaceAsync` for atomic rewrite), `SetupService` now wraps the DEK with the mnemonic too, `ISeedRecoveryService`/`SeedRecoveryService` (recover / enable / is-enabled) + `SeedRecoveryUnavailableException` + `InvalidRecoverySeedException` + `VaultSeedDerivation.Passphrase = "Coffer"`; registered in `AddCofferLogin`. 11 new Security tests; full suite 664 green (was 654). Headless.
- `--:--` note: `SetupService` gained an `ISeedManager` ctor param (DI auto-satisfies via `AddCofferCrypto`; `SetupServiceTests` updated). Existing `LoginServiceTests` DEK helper switched to a **v1** file so it also asserts back-compat (LoginService opens a legacy password-only vault); the DEK-file round-trip test is now a real v2 round-trip plus a v1 test.
- `--:--` decision: rewrite path uses `DekFile.WriteReplaceAsync` (temp → `File.Move` overwrite) so a crash mid-rewrite never destroys the only key file; the first-ever setup write stays `FileMode.CreateNew` (TOCTOU guard). Passphrase pinned as an internal constant reused by setup/enable/restore so they cannot drift.
- `--:--` 25-B complete — restore-from-seed UI: `RestoreFromSeedViewModel` + `RestoreFromSeedWindow` + a "Forgot password? Restore from seed" link on the login window; `App.OnRestoreFromSeedRequested` shows it modally and routes into the app on `RecoveryCompleted`. Seed whitespace/case-normalized before validate + service call. 7 new VM tests (+ `Login.ForgotPassword`/`Restore.Seed.*` i18n, parity green); full suite 671 green (was 664).
- `--:--` decision: no dialog-service seam this time (unlike Sprint-24 restore) — the recovery window is login-adjacent, so `LoginViewModel` raises `RestoreFromSeedRequested` and `App` (which already wires `LoginCompleted`) opens the window and drives navigation, mirroring the existing login flow. Password rules are duplicated from `MasterPasswordStepViewModel` (len≥12, ≥3 classes, score≥3, match, ≠ seed) rather than refactoring the setup VM in this PR.
- `--:--` 25-C complete — enable-seed-recovery in Settings: `EnableSeedRecoveryViewModel` (generate → display → verify a fresh seed → `EnableSeedRecoveryAsync`) reusing the setup display/verification step VMs; `EnableSeedRecoveryWindow` + `IEnableSeedRecoveryDialog` seam (`EnableSeedRecoveryDialogService` applies the screen-capture blocker on open); a "Recovery seed" card in Settings showing state + the enable action. 6 new VM tests; full suite 677 green (was 671). `Settings.SeedRecovery.*` i18n (parity green).
- `--:--` decision: enable dialog uses a Sprint-24-style `IEnableSeedRecoveryDialog` seam (Settings opens a full window with a seed grid), unlike 25-B's login-adjacent event — because Settings is deep in the app and the flow needs display+verify, a dedicated modal window is cleaner. Capture-blocking wired in the dialog service on `window.Opened` (resolves `IScreenCaptureBlocker`), not via the setup wizard's bubbling event.
- `--:--` sprint closed — 25-A (#200) + 25-B (#202) + 25-C merged. The DEK is genuinely dual-wrapped; a forgotten master password is recoverable from the BIP39 seed (login window), new vaults get it automatically, and the existing v1 vault can opt in (Settings, minting a fresh functional seed). Automated DoD met (full suite 677 green); the manual click-through (25.17) is deferred to manual as in prior UI sprints (needs a running app + a real vault + the real seed). **Total data loss on a forgotten password is no longer possible once seed recovery is enabled.**
