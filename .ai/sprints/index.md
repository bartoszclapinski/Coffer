# Sprint index

Status as of: 2026-07-01 (sprint-17 closed; sprint-18 planned)

| Sprint | Phase | Goal | Status |
|---|---|---|---|
| [sprint-0](sprint-0/sprint-0.md) | 0 | Repo on GitHub, `Coffer.sln` with 5 production + 3 test projects, `dotnet build` green, CI green | Closed (2026-05-12) |
| [sprint-1](sprint-1/sprint-1.md) | 0 | DI plumbing, Serilog (console + file), Avalonia booted via DI, `MainWindow` with `ILogger<MainWindow>` | Closed (2026-05-13) |
| [sprint-2](sprint-2/sprint-2.md) | 0 | `IKeyVault` in Core, `WindowsDpapiKeyVault` (DPAPI, 7-day TTL), `InMemoryKeyVault` (cross-platform fallback), round-trip tests | Closed (2026-05-13) |
| [sprint-3](sprint-3/sprint-3.md) | 0 | `Argon2KeyDerivation`, `Bip39SeedManager`, `AesGcmCrypto`, `DekFile` format + serializer, 17 tests (including official BIP39 vectors) | Closed (2026-05-14) |
| [sprint-4](sprint-4/sprint-4.md) | 0 | `CofferDbContext` + `_SchemaInfo`, `SqlCipherKeyInterceptor` (PRAGMA key per connection), `MigrationRunner` with pre-migration backup callback, first migration `InitialCreate`, 9 integration tests | Closed (2026-05-17) |
| [sprint-5](sprint-5/sprint-5.md) | 0 | Avalonia setup wizard (welcome → master password with zxcvbn → BIP39 display + verification → confirm), `IDekHolder`, `SetupService` orchestrator, `IScreenCaptureBlocker` (Win32 P/Invoke); first interactive UI | Closed (2026-05-18) |
| [sprint-6](sprint-6/sprint-6.md) | 0 | `ILoginService` (DPAPI cache → password fallback), `ILastActivityTracker` + `AutoLockMonitor` (15-min idle), `LoginWindow`, `MainWindow` upgrade ("Zalogowano" + version + "Wyloguj"); closes Phase 0 | Closed (2026-05-20) |
| [sprint-7](sprint-7/sprint-7.md) | 1 | Parsing foundations: `IStatementParser` / `IBankDetector` / `StatementParserRegistry`, Polish-format helpers, `FingerprintBankDetector` (PKO + 6 inert), `PkoBpStatementParser` standard checking, `TransactionHash`, FsCheck property-based tests + synthetic QuestPDF fixtures | Closed (2026-05-31) |
| [sprint-8](sprint-8/sprint-8.md) | 1 | Pivot PKO parsing to the "Historia rachunku" **CSV** export: generalise `IStatementParser` / `IBankDetector` off `PdfDocument` → `StatementInput`, `PkoHistoriaCsvParser` (Windows-1250, signed amounts, multi-column description), remove the speculative PKO PDF parser, synthetic golden CSV + real-CSV manual harness | Closed (2026-05-31) |
| [sprint-9](sprint-9/sprint-9.md) | 2 | Import flow + transaction list: domain entities (`Account` / `Transaction` / `ImportSession` / minimal `Category`) + first schema migration applied at startup with pre-migration backup, `ImportStatementUseCase` (parse → dedup → save), `IFilePicker`, Avalonia Import page (drag-and-drop + 5-step progress) and Transactions list (6-month default, search/account/category filters) | Closed (2026-06-05) |
| [sprint-10](sprint-10/sprint-10.md) | 4 | Auto-categorisation: `Rule` / `CategoryCache` / `AiUsageEntry` schema + migrations, deterministic `RuleEngine` + learned cache wired into import, manual re-categorisation in the grid, default Polish category/rule seed, `IAiProvider` (Claude + OpenAI) + `PromptAnonymizer` + cost ledger + `AiBudgetGate` + Settings, `HybridCategorizer` (cache → rules → AI batch). Three phased PRs (10-A deterministic → 10-B AI plumbing → 10-C hybrid) | Closed (2026-06-07) |
| [sprint-11](sprint-11/sprint-11.md) | 6 | Dashboard and charts: `IDashboardQuery` + server-side (`GROUP BY`) aggregations, `DashboardViewModel`, Avalonia Dashboard page (current-month spend/income/net KPI cards, 30-day spend trend, 12-month bar, top-categories doughnut, recent transactions) via LiveCharts2; Dashboard becomes the post-login landing page. Single PR. With no explicit as-of the dashboard anchors on the latest transaction's month so an idle current month does not render empty | Closed (2026-06-07) |
| [sprint-12](sprint-12/sprint-12.md) | 7 | Chat with data: extend `IAiProvider`/`AiRequest` for tool calling, four read-only EF-backed financial tools (`GetTotalSpent` / `GetTransactions` / `GetSpendingByCategory` / `GetMonthlyTrend`), `ChatService` tool-call loop (reasoning model, budget gate, ledger per turn, anonymised tool output), and an Avalonia chat page with a per-response tool-trace. Anomaly/goals/receipt tools deferred to their phases. Two PRs (12-A plumbing+tools+service → 12-B UI) | Closed (2026-06-22) |
| [sprint-13](sprint-13/sprint-13.md) | 8 | Anomalies and alerts: domain `Alert` + five statistical detectors (high-amount z-score, new-merchant, category-spike, duplicate-payment, missing-recurrence) over a 30-day recent / 6-month baseline window, a detection use case (post-import + manual rescan, signature-deduped) persisting alerts with templated Polish text, and an Avalonia Alerty page with accept/dismiss. Then an `AnomalyCommentator` (LLM title/description for the top 10, budget-gated, ledgered as `anomaly-comment`, anonymised) and a `FindAnomalies` chat tool. Mobile push deferred. Two PRs (13-A engine+UI → 13-B AI+chat) | Closed (2026-06-23) |
| [sprint-14](sprint-14/sprint-14.md) | 9 | Financial advisor: domain `Goal` / `GoalContribution` / `GoalSnapshot`, a deterministic `GoalFeasibilityEngine` with one strategy per goal type (purchase, large-expense, emergency-fund, mortgage-prepayment, investment, long-term), a `MortgagePrepaymentCalculator`, and a `FinancialContext` builder over the transaction history, then an Avalonia **Doradca** page (goals CRUD, simulator slider, scenarios, 12-month projection chart), then an `AdvisorReportGenerator` (LLM risks + grounded cutting suggestions, budget-gated, ledgered as `advisor-report`, anonymised), a daily snapshot job, and the `GetGoals` chat tool deferred in Sprint 12. Engine calculates, AI explains. Three PRs (14-A engine → 14-B UI → 14-C AI+chat) | Closed (2026-06-25) |
| [sprint-15](sprint-15/sprint-15.md) | — | Bilingual UI (PL + EN) i18n with a runtime language switch: `ILocalizer` + `.resx` (English neutral, Polish satellite) + Avalonia `{l:Localize}` markup extension refreshing live on change, language persisted in a plaintext app-data file (pre-login readable), a switch in Ustawienia, then migrate all 16 views + VM-formatted strings + deterministic engine/domain display text off hardcoded Polish. Money stays `pl-PL` "zł". Three PRs (15-A foundation+pilot → 15-B views+VM → 15-C engine strings+sweep) | Closed (2026-06-29) |
| [sprint-16](sprint-16/sprint-16.md) | — | Timing-aware cash-flow planning + AI assistant: a persisted, user-editable `RecurringFlow` model (bidirectional, with an accrual offset), detection that proposes flows from history, and a deterministic `CashFlowProjectionEngine` producing a dated inflow/outflow timeline with a running balance (seeded from the statements' running sum), tight-window warnings, a statement-continuity check, and per-event accrual badges; an Avalonia "Plan przepływów" page; and a read-only chat tool + `CashFlowExplainer` (engine calculates, AI explains). Three PRs (16-A engine+detection+persistence → 16-B planning UI → 16-C AI) | Closed (2026-06-30) |
| [sprint-17](sprint-17/sprint-17.md) | 1 | AI-assisted fallback statement parser (deferred since Sprint 8): `AiAssistedParser` (`BankCode = "AI_FALLBACK"`, `CanHandle => true`) wired as the registry's explicit fallback so an unknown bank imports without a code change — one Sonnet-4.6 `CompleteJsonAsync` extraction, `Confidence = Medium` + review warning, empty account (import confirms it). Off by default behind an Ustawienia opt-in (sends statement text to AI); owner-name redaction added to `PromptAnonymizer` from an optional `AppSetting` (no migration); budget-gated + metered as `parser-fallback`. Two PRs (17-A parser+registry+anonymisation+gating → 17-B settings toggle+import warning) | Closed (2026-07-01) |
| [sprint-18](sprint-18/sprint-18.md) | — | Real-balance anchoring + "can I afford this today?": manual `(AnchorDate, AnchorBalance)` on `Account` so `IRunningBalanceQuery` derives the *real* per-account balance forward from statements (relative running sum today), a continuity-trust signal that flags the answer uncertain when a statement gap sits in the window, a deterministic `AffordabilityEngine` (proposed spend + next-inflow projection + recurring flows + a conservative daily-burn allowance + an owner safety floor → afford/not + headroom + what pushes you under), a `CanIAfford` chat tool (zero API calls — engine calculates, AI explains), and planning-page/anchor/settings UI. Three PRs (18-A anchor+balance+trust → 18-B engine+floor+burn+tool → 18-C UI) | Planned |

**Phase 0 closed end-to-end on 2026-05-20.** Cold-start setup → wizard → `MainWindow`; subsequent cold start → DPAPI cache bypass OR login window → `MainWindow`; 15-min idle → auto-lock → login. ~104 automated tests, CI green.

**Phase 1 opened with Sprint 7** — format-agnostic parsing foundations (interfaces, Polish helpers, registry, dedup hash) plus a deterministic PKO BP **PDF** parser for the "Wyciąg z rachunku" layout. Manual verification then revealed the freely-available PKO export is **"Historia rachunku"** (CSV/PDF/XML), not the paid "Wyciąg z rachunku" — so the PDF parser is speculative (synthetic-verified only). **Sprint 8 closed that pivot** — `IStatementParser` / `IBankDetector` now operate on a format-neutral `StatementInput` (PdfPig dropped from `Coffer.Core`), the speculative PKO PDF parser is gone, and `PkoHistoriaCsvParser` parses the "Historia rachunku" CSV (Windows-1250, signed amounts, joined descriptions), verified by a committed synthetic golden CSV in CI and a gitignored real CSV manually (39 transactions). The AI fallback, Anonymizer CLI, and remaining PKO layouts follow. See [sprint-7/log.md](sprint-7/log.md) for the finding and the CSV schema.

**Phase 4 closed with Sprint 10** — hybrid auto-categorisation (deterministic rules + learned cache, falling back to batched AI behind a budget gate). **Phase 6 shipped with Sprint 11** — a post-login Dashboard summarising the current month and recent spending, charted with LiveCharts2 over server-side aggregations. With Phases 0, 1, 2, 4, and 6 done the desktop MVP is end-to-end: setup/login → import → categorise → review → dashboard.

**Phase 7 closed with Sprint 12** — a Polish-language chat assistant that answers questions about the owner's finances by calling four read-only EF-backed tools (`GetTotalSpent` / `GetTransactions` / `GetSpendingByCategory` / `GetMonthlyTrend`) through a tool-call loop, never inventing numbers, metering every turn into the cost ledger behind the monthly budget gate, with anonymised tool output and a collapsible per-message tool-trace. Two PRs (12-A plumbing+tools+`ChatService` → 12-B "Asystent" chat page + shell wiring). Anomaly/goals/receipt tools slot in at their own phases without touching the orchestrator.

**Phase 8 closed with Sprint 13** — anomaly detection and alerts. Five in-memory statistical detectors (high-amount z-score, new-merchant, category-spike, duplicate-payment, missing-recurrence) run over a rolling 30-day recent / prior-6-month baseline window; a signature-deduped detection service persists findings as `Alert` rows with templated Polish text (a dismissed signature is never resurrected), surfaced on an Avalonia **Alerty** page with accept/dismiss. An `AnomalyCommentator` then rewrites the title/description of the top 10 findings per run with a single budget-gated, anonymised, reasoning-tier LLM call metered once as `anomaly-comment`, falling back to the templated text on any failure. The `FindAnomalies` chat tool deferred in Sprint 12 lands here, returning active alerts whose period overlaps a date range. Two PRs (13-A engine+UI → 13-B AI commentary + chat tool). With Phases 0, 1, 2, 4, 6, 7, and 8 done the desktop app spans setup/login → import → categorise → review → dashboard → chat → alerts.

**Phase 9 closed with Sprint 14** — the financial advisor. Domain `Goal` / `GoalContribution` / `GoalSnapshot` feed a deterministic `GoalFeasibilityEngine` with one strategy per goal type (purchase, large-expense, emergency-fund, mortgage-prepayment, investment, long-term) plus a `MortgagePrepaymentCalculator`, all scored against a `FinancialContext` derived from the transaction history (6-month moving averages). An Avalonia **Doradca** page renders the projection with a live simulator slider, alternative scenarios, and a 12-month chart. On top, an `AdvisorReportGenerator` turns the engine's verdicts into LLM-written per-goal risks and grounded cutting suggestions (budget-gated, anonymised, metered once as `advisor-report`, engine-only fallback on any failure), persisted as a daily `AdvisorReport` by a once-a-day desktop-startup snapshot job; the `GetGoals` chat tool deferred in Sprint 12 lands here. **The engine calculates; the AI only explains** — every number is deterministic C#. Three PRs (14-A engine → 14-B Doradca page → 14-C AI + chat tool). With Phases 0, 1, 2, 4, 6, 7, 8, and 9 done the desktop app spans setup/login → import → categorise → review → dashboard → chat → alerts → advisor.

**Sprint 15 closed the bilingual UI (PL + EN) i18n** — a cross-cutting effort (not a roadmap phase) giving the whole desktop app a runtime language switch with no restart. An `ILocalizer` over `Strings.resx` (English neutral) + `Strings.pl.resx` (Polish satellite) is resolved in markup through an `{l:Localize}` extension that refreshes live on change; the choice persists in a plaintext app-data file readable before the DEK exists, so setup/login honour it too. Migration ran in three PRs: 15-A foundation + pilot (`MainWindow` nav + Settings switch), 15-B every remaining view + VM-formatted status/error strings + app dialogs, 15-C the deterministic engine-origin display text (goal scenario/risk labels emitted as stable codes and resolved at the VM boundary, keeping `Coffer.Core` presentation-free) plus a resx key-parity test and the `docs/conventions.md` policy update. Money/number formatting stays `pl-PL` + " zł" regardless of UI language (v1). **Deferred:** runtime localization of anomaly alert text (baked into the DB at detection time, AI-rewritten as single-language prose) is tracked as a follow-up (#128).

**Sprint 16 closed timing-aware cash-flow planning + an AI assistant** — a beyond-roadmap sprint replacing the flat "income − fixed − variable" monthly figure with a within-month picture of *what is paid when and when money comes in*. A persisted, user-editable `RecurringFlow` model (bidirectional, with an owner-set accrual offset) is proposed from history by a detector that recycles the recurrence grouping previously trapped in `MissingRecurrenceDetector`; a deterministic `CashFlowProjectionEngine` in `Coffer.Core` expands active flows over a selectable horizon (default 60 days) into a dated inflow/outflow timeline with a running balance seeded from the statements' running sum, tight-window warnings, and per-event accrual badges, while a `StatementContinuityChecker` flags import gaps that would corrupt that balance. An Avalonia **Plan przepływów / Cash flow** page renders the timeline and a LiveCharts2 running-balance line; a read-only `GetCashFlowProjection` chat tool and a `CashFlowExplainer` (budget-gated, anonymised, metered once as `cashflow-explain`, engine-only fallback) let the assistant narrate the timing in prose. **The engine calculates; the AI only explains** — every number is deterministic C#. Three PRs (16-A engine+detection+persistence → 16-B planning UI → 16-C AI tool + explainer). **Deferred:** a variable-spend daily-burn overlay, a monthly accrual rollup, and per-account reconciled-balance anchoring.

**Sprint 17 closed the AI-assisted fallback statement parser** — the Phase-1 item deferred since Sprint 8. An `AiAssistedParser` (`BankCode = "AI_FALLBACK"`, `CanHandle => true`, `Confidence = Medium`) is wired as the `StatementParserRegistry`'s explicit `_fallback`: deterministic parsers still win for known banks, and only the unknown-bank slot routes through a single Sonnet-4.6 `CompleteJsonAsync` extraction that leaves the account number empty (import confirms it) and flags a mandatory "parsed by AI fallback — review" warning. It is **off by default behind an explicit Ustawienia opt-in** — sending whole statement text to a third-party model is the most data-exposing AI feature in the app, so it requires consent; when disabled the registry throws `UnsupportedBankException` exactly as before. `PromptAnonymizer` gained owner-name redaction sourced from an optional `AppSetting` (no migration); when that name is unset the parser still runs with account/IBAN/NIP redaction plus a non-blocking header-exposure warning. The fallback is budget-gated and metered as `parser-fallback`. Two PRs (17-A parser+registry+anonymisation+gating, headless — #148; 17-B Ustawienia toggle+owner-name field+localized import review/header banners — #150). Because Application cannot reference Infrastructure, the raw English parser warnings are suppressed and re-surfaced as structured `AiFallbackUsed` / `OwnerNameUnredacted` flags on `ImportSummary`, rendered as localized banners. **Deferred to manual (needs a real API key + real statement):** the gitignored real-unknown-bank harness run and the DoD click-through.

## Beyond Phase 1

- **Phase 2** — Import flow + transaction list (done, Sprint 9)
- **Phase 3** — Sync via Google Drive (not started)
- **Phase 4** — Auto-categorisation (done, Sprint 10)
- **Phase 5+** — Receipts (from this phase, mobile/MAUI is needed)
- **Phase 6** — Dashboard and charts (done, Sprint 11)

Mobile (`Coffer.Mobile`, MAUI) — postponed until Phase 5 is approaching or it is needed earlier.

> Note: Sprint plans and logs for Sprints 0-3 are in Polish (historical artefacts from the early phase of the project). All sprints from Sprint 4 onward are in English. See [.ai/sprints/README.md](README.md) for the language policy.
