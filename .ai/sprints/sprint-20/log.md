# Sprint 20 log

## 2026-07-02

- `--:--` sprint planned — category budgets with mid-month tracking (budget zones). Beyond-roadmap; the owner picked it over what-if runway, spending-explorer polish, and backup/recovery. Sits on Sprint-19's per-category debit-sum aggregation, Sprint-11's current-month anchor, and (optionally) Sprint-13's alert engine. A `CategoryBudget` entity (named to stay distinct from the `AiBudgetGate` cost cap) + a pure `BudgetTrackingEngine` + a page. Two committed PRs: 20-A (entity + backed-up migration + engine + query, headless) → 20-B (Budgets page + nav + i18n); an optional 20-C (over-budget alert detector + `GetBudgetStatus` chat tool) only if the first two land comfortably.
- `--:--` decision: **calendar-month, linear projection, zones at 80% / 100%-or-projected-over**; one recurring monthly limit per (category, currency), no per-month override or rollover in v1; uncategorised month spend shown as an unbudgeted line so overspending can't hide. Engine calculates in `Coffer.Core`; the query only assembles inputs. No AI, no new cost. The `CategoryBudgets` migration runs `pre-migration-backup` (hard rule #8) — the first migration since Sprint 18.
- `--:--` 20-A complete (steps 20.1–20.7) — headless core. `CategoryBudget` entity + `CategoryBudgetConfiguration` (FK to `Category`, cascade delete, indexes on `CategoryId`/`IsActive`) + DbSet; migration `20260702182230_AddCategoryBudgets` generated via `dotnet ef` (design factory). Pure `BudgetTrackingEngine` + records (`BudgetStatus`/`BudgetZone`, `BudgetLine`/`UnbudgetedLine`/`BudgetOverview`, `CategoryBudgetItem`) in `Coffer.Core/Budgeting/`. `CategoryBudgetRepository` (upsert one active per category+currency, hard delete, unknown-category throws) + `BudgetTrackingQuery` (dashboard-anchored month, per-category month-to-date debit sums → engine → budget lines + unbudgeted incl. the uncategorised bucket, optional account scope) in `Coffer.Infrastructure/Budgeting/`. Registered via a new `AddCofferBudgeting` (engine singleton). Tests: 6 Core (engine zones/projection/guards) + 7 Infrastructure (4 repo + 3 query over real SQLCipher). Full suite green (587: 32 Core + 148 Application + 407 Infrastructure). UI lands in 20-B.
- `--:--` problem: the new migration broke `MigrationRunnerTests` (it pins the full ordered migration list + the latest name) → resolution: added `ExpectedAccountAnchorMigration` and repointed `ExpectedLatestMigration` to `AddCategoryBudgets`, extended the expected list. Expected — the test intentionally guards the migration history.
- `--:--` problem: `GetActiveAsync` projected a `Join` straight into the `CategoryBudgetItem` record → `InvalidOperationException` (EF can't translate constructing the record in an ordered `Join`) → resolution: project to an anonymous type server-side, order, then map to the record in memory (the tracking query already used an anonymous projection, so it was unaffected).
