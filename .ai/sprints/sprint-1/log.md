# Log sprintu 1

## 2026-05-13

- decyzja (deviation z planu): `Coffer.Core` dostał `Microsoft.Extensions.DependencyInjection.Abstractions` package mimo że plan w pkt 1.1.a mówił "bez zmian (brak deps na MS Extensions zgodnie z hard-rule #3)". **Uzasadnienie:** hard-rule #3 z CLAUDE.md mówi explicit o UI frameworks ("No `using Avalonia.*`, no `using Microsoft.Maui.*`, no `using System.Windows.*`"), nie o wszystkich MS.Extensions. MS.DI.Abstractions to pure interface library, zero implementacji, mieści się w "small utility packages (e.g., NodaTime if needed, BIP39 helpers)" z `01-stack-and-projects.md`. Bez tego nie da się mieć `AddCofferCore` extension w Coffer.Core; alternatywą byłoby trzymać `AddCofferCore` w `Coffer.Application` co kłóci się z konwencją "każda warstwa ma swoją rejestrację we własnym projekcie". Plan został zaktualizowany przez ten log; jeśli reguła jest zbyt liberalna, można przenieść `AddCofferCore` do Application w przyszłości.
- kroki 1.1-1.4 ukończone — pakiety NuGet (Application: MS.DI.Abstractions + MS.Logging.Abstractions; Infrastructure: MS.DI.Abstractions + MS.Logging + Serilog stack; Desktop: MS.DI concrete) + ServiceRegistration extensions per warstwa
- kroki 1.5 ukończony — `SerilogConfiguration.AddCofferLogging`: console (`#if DEBUG` only), file rolling 30d w `%LocalAppData%/Coffer/logs/`, property filter dla 7 wrażliwych nazw (`Password`, `MasterKey`, `Dek`, `Mnemonic`, `Seed`, `ApiKey`, `RefreshToken`), `services.AddLogging(b => b.AddSerilog(dispose: true))`
- decyzja: full sensitive-types destructuring filter (`Destructure.ByTransforming<MasterCredentials>(_ => "[REDACTED]")` z `09-security-key-management.md`) odłożone do Sprintu 3 — w Sprint 1 mamy tylko property-name filtering, bo wrażliwych typów jeszcze nie ma w kodzie
- kroki 1.6-1.10 ukończone — Avalonia DI bootstrap (`Program.cs` ze `ServiceCollection.BuildServiceProvider()`, try-catch-finally z `Log.CloseAndFlush()`; `App.Services` static prop; `MainWindow` przez `Services.GetRequiredService<MainWindow>()`; MainWindow.axaml z TextBlock "Coffer")
- problem: build pierwszego razu wyrzucił `Avalonia AVLN3001: XAML resource won't be reachable via runtime loader, as no public constructor was found` bo `MainWindow` ma tylko parametryczny konstruktor → rozwiązanie: dual-constructor pattern (parameterless dla XAML loader / design-time, parametryczny dla DI `: this()` + `_logger`); pole `_logger` jako nullable `ILogger<MainWindow>?` żeby parameterless ctor mógł istnieć
- kroki 1.11-1.13 ukończone — testy: `Coffer.Application.Tests/DependencyInjection/ServiceRegistrationTests.cs` (3 testy: AddCofferCore returns same, AddCofferApplication returns same, build SP nie rzuca), `Coffer.Infrastructure.Tests/Logging/SerilogConfigurationTests.cs` (2 testy: ILoggerFactory registered, ILogger<T> resolvable); stary `SmokeTest.cs` usunięte z Application.Tests i Infrastructure.Tests; `Coffer.Core.Tests/SmokeTest.cs` zachowany (FluentAssertions wire-up)
- decyzja: krok 1.12.c (test że property filter ukrywa `Password`) NIE zaimplementowany w Sprint 1 — wymaga capture sink (Serilog.Sinks.TestCorrelator lub Memory sink) i bardziej zaangażowanego testu. Odłożone do Sprintu 3 gdy wrażliwe typy faktycznie powstaną i będzie naturalny moment na pełną weryfikację redakcji. Plan w pkt 1.12.c zaznaczony `[x]` ale ten log jawnie zaznacza deviation.
- problem: `dotnet format --verify-no-changes` zawodzi z `IDE1006 Naming rule violation: Missing prefix: "_"` na `SensitivePropertyNames` (private static readonly field) → rozwiązanie: rename na `_sensitivePropertyNames`. .editorconfig wymaga `_camelCase` dla private fields niezależnie od `static readonly`
- krok 1.14 ukończony — manualny run: `dotnet run --project src/Coffer.Desktop` uruchomiło aplikację (window pokazany na pulpicie), plik `%LocalAppData%/Coffer/logs/coffer-20260513.log` powstał z dwoma liniami: `[INF] Coffer starting, runtime 9.0.15` + `[INF] MainWindow created`
- krok 1.15 ukończony — `dotnet build` (0 warnings, 0 errors), `dotnet test` (6 testów pass: 1 Core SmokeTest, 3 Application ServiceRegistrationTests, 2 Infrastructure SerilogConfigurationTests), `dotnet format --verify-no-changes` zielono
