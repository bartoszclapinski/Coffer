# Sprint 23 log

## 2026-07-03

- `--:--` sprint planned — local backup: daily encrypted-DB snapshots with rolling retention, a Settings "Backup & Recovery" status panel with "Backup now", and a manual "Export full archive" (`.zip` of the encrypted DB + encrypted DEK + a manifest). Closes the single-file (`%LocalAppData%\Coffer\coffer.db`) data-loss risk — the largest and cheapest-to-close gap, since the file is already SQLCipher-encrypted (a backup is a copy). Doc 08, Layer 1 + Layer 3 + the Settings panel. No schema, no AI, no network.
- `--:--` decision: **backup only this sprint; restore is its own sprint.** Snapshots + export are self-contained and low-risk; guided restore (from snapshot / from BIP39 seed) touches the setup/login flow and DEK re-encryption — riskier and larger. Until then a snapshot is recoverable by hand (copy the dated `.db` back) and the archive is the offline package; the Settings panel states this. Layer 2 Google Drive and the monthly auto-test restore are also deferred (Drive depends on the not-started Phase-3 sync spine).
- `--:--` decision: **a snapshot is a file copy of the already-encrypted DB + its `-wal`/`-shm` side-files** (no re-encryption, no `VACUUM INTO`) — the technique `PreMigrationBackup` already uses. A shared `BackupSnapshotWriter` owns the copy so the daily service and the pre-migration hook share one tested path; `PreMigrationBackup` is refactored onto it with behaviour unchanged (its byte-for-byte tests guard that). Daily snapshot writes `.tmp` then atomic `File.Move`; naming `coffer-{YYYY-MM-DD}.db`; retention by a **pure** `BackupRetention` (30d daily, 90d pre-migration — the pre-migration folder gains the rotation it lacks today), parsed from filenames so it needs no DB and works before login.
- `--:--` decision: **triggered as a delayed startup `Task.Run`** (mirroring `StartDailyAdvisorRefresh`), not a hosted service (none exist); idempotent within a day; failures logged and swallowed. Status is read from the filenames on disk, not the encrypted store, so the panel works pre-DEK. **Direct `System.IO` behind `IVaultPaths`** (no `IFileSystem` in the codebase; hard rule #4 satisfied at the path + picker level) — the `WindowsDpapiSecretStore`/`PreMigrationBackup` pattern. `IFilePicker` gains a save-file method (it is open-only today) for the archive.
- `--:--` decision: **two PRs — 23-A headless (service + retention + shared writer + exporter + DI) → 23-B Settings panel + `IFilePicker` save + startup scheduler + i18n.**
- `--:--` open questions recorded for the owner: scope (backup-only now, restore next — proposed), "Backup now" semantics (create/refresh today's local snapshot — proposed, since Drive is deferred), and the snapshot trigger delay.
- `--:--` decision (owner): **backup first** — restore is a separate later sprint. Confirmed the sprint scope.
- `--:--` 23-A complete (steps 23.1–23.7) — headless service. `Coffer.Core/Backup/`: `IBackupService`/`IArchiveExporter`, records (`BackupStatus`, `BackupResult`), and a pure `BackupRetention` (parses `coffer-{yyyy-MM-dd}.db` daily and `coffer-{yyyyMMddTHHmmssZ}.db` pre-migration names, selects those older than a keep-days window; malformed names ignored). Shared `BackupSnapshotWriter` (`Coffer.Infrastructure/Backup/`) copies `coffer.db` + `-wal`/`-shm`; `PreMigrationBackup` refactored onto it (its byte-for-byte + side-file tests stay green). `BackupService`: `CreateDailySnapshotAsync` (idempotent within a day — skip if today's file exists; else write a `.tmp` set via the writer then `File.Move` into place, .db last), `CreateSnapshotNowAsync` (force-refresh), `GetStatusAsync` (last daily date/count + newest pre-migration write-time, all from disk); prunes daily >30d and pre-migration >90d each pass. Deliberately no `ClearAllPools` (can run live) — the WAL set is copied alongside. `ArchiveExporter` zips `coffer.db` (+ side-files) + `dek.encrypted` + a `manifest.json` (app version + UTC timestamp) via `System.IO.Compression`. New `AddCofferBackup` (both singletons) chained into `AddCofferInfrastructure`. Tests: 6 Core (retention boundary + both parsers) + 9 Infrastructure (7 `BackupService` incl. idempotency/rotation/status + 2 `ArchiveExporter`) over `TestVaultPaths`. Full suite green (629: 45 Core + 160 Application + 424 Infrastructure). UI + export picker + scheduler land in 23-B.
- `--:--` note: `dotnet format` flagged import order in `ServiceRegistration.cs` after adding the two backup usings — auto-fixed, no other files touched.
