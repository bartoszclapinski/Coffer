# Sprint 29 log

## 2026-07-10

- `--:--` decision (owner): after Sprint 28 (foundation + shell + Overview), migrate **every remaining screen** to the design system so the whole app is cohesive for a demo video ("rób po kolei wszystko, jak skończymy to nagram"). Record in **either** theme → migrate for **both** (default flips to Dark at the end). The new `Accounts` screen is deferred (it's a feature, not a reskin).
- `--:--` plan written (`sprint-29.md`), Status: Planned. Five grouped PRs by screen family: 29-A ledgers (Transactions + Spending) → 29-B budgets & forecast → 29-C advisor & planning → 29-D import/alerts/chat → 29-E Settings + pre-login windows + flip default to Dark. Each screen validated in both themes via env-var preview harnesses (no login/DB). Pure reskin — no VM/data/schema/AI change. Plan PR #222 merged (also closed Sprint 28).
- `--:--` 29-A complete (Transactions + Spending). `TransactionsView` on tokens: token filters, the DataGrid wrapped in `Border.panel` (Fluent's DataGrid theme is already variant-aware, so dropping the hardcoded white/grey lets it follow light/dark), amounts on the `money` class + privacy blur. `SpendingExplorerView` on tokens: panels, `label`/`muted`, drill-down rows (hover → `Coffer.Hover`), `ProgressBar.bar` share bars, `money` totals + blur, `chip` back/apply. Transactions preview harness (`COFFER_TX_PREVIEW` + canned stub query/service) captured in **both** themes; Spending reuses only already-proven components so a dedicated harness was skipped.
- `--:--` fix (cross-cutting, real bug): amounts used `AmountToBrushConverter`, which coloured expenses `#1D1D1F` (near-black) — readable on light paper but **invisible on the dark panel**. Replaced with the theme-aware `money` class + `Classes.pos`/`Classes.neg` toggled by new `AmountConverters.IsPositive`/`IsNegative`, so income is green (`Pos` token) and expenses are `Tx` (white on dark / near-black on light). Applied to Transactions **and** back-ported to Overview (Net KPI + recent amounts) — verified crisp in dark.
- `--:--` 29-B complete (Budgets + Forecast). `BudgetsView` on tokens: `Border.panel` cards, `label`/`money` metric grid (Spent/Limit/Remaining/Projected) + privacy blur, zone `ProgressBar.bar` with the data-driven `ZoneColor` override, the zone badge kept (white text on the semantic colour), `accent` save + `chip` remove. `ForecastView` same recipe: per-category fixed/variable/suggested/current + total, `chip` set-as-budget. Only intentional hex left is the `#FFFFFF` badge text (readable on the coloured zone in both themes). Dedicated previews skipped — every element is already-proven (panel/label/money/bar/chip + data colours from the 28-A gallery and 29-A); build clean, no stray hex.
- `--:--` 29-D complete (Import + Alerts + Assistant/chat). `ImportView` and `AlertsView` migrated in parallel (two subagents, shared recipe): hand-rolled `#FFFFFF/#E5E5EA` cards → `Border.panel`, big page titles dropped (the shell top bar owns them), field captions → `muted`, section headers → `title`, alert amount → `money` + privacy blur, buttons → `accent`/`chip`, error `#D14` → `Coffer.Neg`. `ChatView` migrated by hand (the novel screen): user bubble on `Coffer.Accent`/`OnAccent`, assistant bubble on `Panel`/`Bd`, suggestion pills + tool-trace on `Panel2`/`Mut2`, send → `accent`. No amber token exists, so the two soft notes split by intent: the AI-fallback note is neutral (`Panel2`/`Mut2`); the privacy warning (owner name unredacted) and the chat blockers (missing API key, budget exceeded) take `NegTint`/`Neg` for visibility. Build clean, zero stray hex across all three. Previews skipped — every element is a proven component or a trivial token-swapped bubble; the VMs would need 4–6 stubbed services each, disproportionate to a reskin.
- `--:--` 29-C complete (Goals/Advisor + Cash-flow Planning + Affordability). The three largest views migrated to tokens in parallel (three subagents, one view each, following the shared recipe): local `.card`/`.sectionTitle`/`.metricLabel`/`.metricValue` styles dropped for `panel`/`label`/`muted`/`money`; big page titles removed (the shell top bar owns them); money figures gained privacy blur; buttons → `accent`/`chip`; progress bars → `bar`; data-driven status pills + LiveCharts series left untouched. Added semantic tint tokens `Coffer.PosTint`/`Coffer.NegTint` (pos/neg at ~13% alpha over the panel, theme-correct like the category tiles) and used them for the Affordability afford/can't-afford verdict banners and the Cash-flow statement-gap alert (replacing a light-red surface that would have been unreadable in dark). Build clean; only intentional `#FFFFFF` badge text remains (white on data-coloured status pills). Previews skipped — the VMs need 5–7 stubbed services each, disproportionate to a reskin built entirely from proven components + trivial tint brushes.
