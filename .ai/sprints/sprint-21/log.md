# Sprint 21 log

## 2026-07-03

- `--:--` sprint planned — the optional **20-C** tail deferred at the close of Sprint 20: an over-budget `IAnomalyDetector` and a read-only `GetBudgetStatus` chat tool, so budget state surfaces on the Alerty page and in chat, not only on the Budgets page. Reuses the Sprint-13 anomaly engine (dedup + persist pipeline, the Alerty page) and the Sprint-12 chat-tool pattern (`ChatTool` base, auto-discovery, central anonymisation). No migration, no UI, no new AI cost.
- `--:--` decision: **alert fires on `Over` only** (month-to-date spend ≥ limit), not `Warning`/approaching — a discrete "you crossed it" event; approaching stays a coloured bar on the Budgets page so alerts don't fire mid-month for every healthy budget. Signature `over-budget:{categoryId}:{yyyyMM}` → one alert per category per month, idempotent across reruns, dismissed never resurrected (existing pipeline behaviour). Alert text stays Polish-templated (baked at detection time, like all Sprint-13 alerts; runtime localization deferred under #128) → no new `.resx` keys.
- `--:--` decision: **keep `IAnomalyDetector` pure** — `AnomalyDetectionService` (already async/DB-bound) calls the existing `IBudgetTrackingQuery.GetOverviewAsync` once and hands the `BudgetOverview` to an extended `AnomalyDetectionContext`; `OverBudgetDetector` just filters `Zone == Over`. Reuses the Sprint-20 query wholesale rather than re-implementing the month-to-date-per-category aggregation, and keeps the persist/dedup pipeline the Alerty page depends on (vs. raising alerts straight from the query, which would fork it).
- `--:--` decision: **one headless PR (21-A)** for both hooks — small, shared theme, `Coffer.Core` + `Coffer.Infrastructure` + tests only; split only if review flags it.
- `--:--` 21-A complete (steps 21.1–21.9) — both hooks. `OverBudget` added to `AnomalyType`; `AnomalyDetectionContext` gained an optional `BudgetOverview? Budgets` (default null, other detectors ignore it); `AnomalyDetectionService` now injects `IBudgetTrackingQuery` and assembles the overview once (dashboard-anchored month, all accounts) into the context. `OverBudgetDetector` filters `Zone == Over`, emitting one candidate per (category, month) with a month-stable signature (`over-budget:{categoryId}:{yyyyMM}`) and Polish templated text (limit/spent/overspend via `AnomalyFormatting.Pln`). `GetBudgetStatusTool` (parameterless) projects the overview — per-category limit/spent/remaining/projected/zone + the unbudgeted lines incl. the uncategorised bucket — to JSON. Two DI lines. Tests: 2 detector unit (only `Over` emits, month-stable signature, null budgets → nothing), 2 service end-to-end over real SQLCipher (one over-budget `Alert`, idempotent within the month), 3 chat-tool (over-zone + unbudgeted bucket, empty-budgets shape, discoverable via `AddCofferChat`). Full suite green (601: 32 Core + 155 Application + 414 Infrastructure). No UI, no migration, no new AI cost.
- `--:--` problem: adding `GetBudgetStatusTool` to `AddCofferChat` made the four existing chat-tool discoverability tests fail — enumerating `IChatTool` now constructs the new tool, which needs `IBudgetTrackingQuery` → resolution: added `services.AddCofferBudgeting()` to each of those tests (mirrors how they already register `AddCofferGoals`/`AddCofferPlanning` for the other tools' deps). Expected — the tests deliberately resolve the whole tool menu.
- `--:--` 21.10 (manual DoD click-through) deferred to manual — needs a running desktop app, real imported data, and an API key for the chat leg. Everything else automated and green.
- `--:--` sprint closed — budget state is no longer trapped on the Budgets page: crossing a category limit raises a dismissable over-budget alert on the Alerty page, and the chat assistant can report per-category budget status on demand via `GetBudgetStatus`, both grounded in the deterministic tracking engine.
