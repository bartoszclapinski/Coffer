# Sprint 18 log

## 2026-07-01

- `plan` sprint-18 drafted — real-balance anchoring + a deterministic "can I afford this today?" affordability answer, the core question the app exists for. Split into 18-A (anchor fields on `Account` + per-account anchor-aware `IRunningBalanceQuery` + continuity-trust signal), 18-B (`AffordabilityEngine` + safety floor + variable-burn query + `CanIAfford` chat tool), 18-C (anchor-editing UI + account selector + affordability panel on the planning page + safety-floor setting).
- `context`: builds directly on Sprint 16. Today `RunningBalanceQuery` sums all PLN transactions across accounts with no anchor (relative, not real); `CashFlowProjectionEngine` has a `tightFloor` param that every caller passes as `0`; variable day-to-day spend is not modelled. Those three are exactly what block "can I spend 2000 today".
- `decision` (owner, confirmed): balance anchor = manually set `(AnchorDate, AnchorBalance)`, then derived forward from imported statements; monthly imports self-update it; a partial-month import (e.g. 1–20) still yields today's balance. Continuity checker already surfaces per-account gaps — when a gap sits between the anchor and today the answer is flagged uncertain, not silently wrong.
- `decision`: order chosen by assistant (both features equally important, owner left it open) — Sprint 18 = affordability + anchor (the heart of the product, foundational balance work), Sprint 19 = spending explorer (selectable-window category breakdown + merchant drill-down; data already present via `Transaction.Merchant`, no migration).
- `decision`: **engine calculates, AI explains** holds — the `AffordabilityEngine` is pure C# and the `CanIAfford` tool makes zero API calls; only optional prose narration costs anything.
- `open question` (for owner): confirm one anchor per account (no history); trailing-3-month single-number variable burn is enough for v1; safety floor is global not per-account; where the affordability answer lives.
- `decision` (owner, confirmed): variable burn = single trailing-3-month average daily spend (no per-category split) — OK for v1; safety floor is a single global buffer in Ustawienia, not per-account; the affordability answer lives on a **dedicated "Can I afford?" page** with its own nav entry, not a panel on the cash-flow planning page. Step 18.13 + files-affected + DoD updated accordingly.
- `still open`: the owner asked what the balance "anchor" is — explained in chat (a one-time "on date X my real balance was Y", from which the app derives the real balance forward). The one-anchor-per-account / no-history point stays open until the owner confirms the concept is understood and accepted.
- `decision` (owner, confirmed): anchor concept understood and accepted — one anchor per account, no history (re-anchoring overwrites). The last plan open question is closed.
- `build` 18-A (#155, `feat/sprint-18a-balance-anchor`): `Account.AnchorDate`/`AnchorBalance` (both nullable) + migration `20260701211515_AddAccountBalanceAnchor` (two nullable columns; runtime `pre-migration-backup` unchanged, MigrationRunner tests updated for the new latest migration). `IRunningBalanceQuery.GetBalanceAsOfAsync` now takes `Guid? accountId`: null = legacy cross-account running sum (existing behaviour preserved), non-null = per-account; anchored accounts return `AnchorBalance + Σ(post-anchor delta)`. New `IBalanceTrustQuery`/`BalanceTrustQuery` (reuses `StatementContinuityChecker`) answers "is [anchor|firstTxn, asOf] contiguous for this account?" → `BalanceTrust(IsTrustworthy, WindowFrom, Gaps)`, presentation-free in Core. Planning VM + `GetCashFlowProjectionTool` pass `accountId: null` for now (per-account selector lands in 18-C). Tests: anchored/unanchored/per-account isolation for the balance query, four trust cases (contiguous, in-window gap, unanchored earliest-txn window, other-account gap ignored). Full suite green (523).
- `decision` (owner, 2026-07-02): 18-C shipped as one PR (issue → branch → full UI); the balance-anchor editor lives in **Ustawienia** (a per-account card), not on a page or dialog — chosen by the owner over the alternatives.
- `build` 18-C (#159, `feat/sprint-18c-affordability-ui`): the UI layer over the 18-A/18-B primitives. New **"Czy mnie stać? / Can I afford?"** page (own nav entry + `AffordabilityViewModel` in `Coffer.Application`) — account selector (incl. all-accounts), amount + optional date → a grounded verdict (afford/not, balance today, safety floor, lowest point + date, headroom, estimated daily burn, next income, the recurring payment that pushes you under) with *relative* (no anchor) and *uncertain* (statement gap) banners. It assembles the engine inputs exactly like `CanIAffordTool` (`IRunningBalanceQuery` + `IVariableBurnQuery` + `IBalanceTrustQuery` + `IPlanningSettings` + active PLN flows) and runs the pure `AffordabilityEngine` — zero AI calls. Ustawienia gained a **safety-floor** field (bound to `IPlanningSettings`) and a per-account **balance-anchor editor** (real balance + as-of date, future-date rejected, set/clear together) backed by two new `IAccountService` methods (`GetAllWithAnchorsAsync`, `SetBalanceAnchorAsync`) + an `AccountAnchorItem` projection. Shell wiring: `MainViewModel` nav entry/command + `MainWindow` DataTemplate/button + Desktop DI. Every new string is a key in **both** `Strings.resx`/`Strings.pl.resx` (parity test green); money stays `pl-PL` "zł". Tests: 4 affordability-VM cases (grounded verdict+driver, relative flag, zero-amount error, uncertain-on-gap), 6 settings-VM cases (floor + anchor round-trips incl. future-date rejection + clear), a MainVM nav case, and 4 infra `AccountServiceTests` (anchor projection + set/clear/unknown over real SQLCipher). Full suite green (554). `AffordabilityEngine` (Core, pure) reuses `CashFlowProjectionEngine` to expand flows, applies the proposed spend as a same-day outflow, overlays a flat daily burn charged up to each event, and walks to the next inflow (relief point) — returns `AffordabilityVerdict(CanAfford, LowestBalance+date, Headroom, Driver="what pushes you under", NextInflowDate, IsUncertain+gap, IsRelative)`; if no inflow appears it burns to a 92-day horizon so the answer stays conservative. Safety floor moved onto a dedicated `IPlanningSettings`/`PlanningDefaults.SafetyFloorPln=0` surface (backed by the existing `AppSettingsStore` KV table, no migration) and threaded into both `Project(..., tightFloor)` call sites (planning VM + `GetCashFlowProjectionTool`), replacing the hard-coded `0`. `IVariableBurnQuery`/`VariableBurnQuery` (Infra) = trailing-3-month average daily discretionary outflow per account, server-side, excluding txns whose merchant/category matches an active flow, `0` on thin history. `CanIAffordTool` (chat, extends `ChatTool`; params amount/date?/accountId?) resolves balance+flows+burn+floor+trust, runs the engine, shapes the verdict as JSON — takes no `IAiProvider`, so structurally zero provider calls; no-account requests are flagged relative. Tests: 7 engine golden verdicts (afford/not-afford/pure-burn/no-inflow-horizon/uncertain/relative/negative-throws), 5 burn cases (merchant+category exclusion, inflow/out-of-window ignored, per-account scope), 5 tool cases (grounded verdict+driver, relative flag, missing-amount+bad-guid errors, DI discoverability). Full suite green (540).
