# Sprint 28 log

## 2026-07-08

- `--:--` decision: the desktop UI redesign (`.ai/design/Coffer desktop project design/` — the "pro terminal" high-fidelity prototype) becomes the priority; Sprint 27 (disaster-recovery tail) is **paused** mid-plan and resumes after the redesign program.
- `--:--` decision (owner): **full shell + remap onto real data**, not a literal rebuild. Adopt the visual language + UX (icon rail, top bar, ⌘K, light/dark tokens, panels, category colors) and map it onto Coffer's actual features/data. The mock's net worth / investments / debt / APY / credit-utilization / Plaid account-linking have no Coffer equivalent and are **not** built — that would violate "not a banking integration / not an investment advisor". **Currency stays PLN "zł"** (`pl-PL`), never `$`. No fabricated data.
- `--:--` decision (owner): **longer icon rail + ⌘K** for the features outside the mock's six screens (Import, Assistant, Alerts, Cash-flow, Affordability, Forecast) — the rail carries more than six icons and the palette reaches everything; nothing is dropped.
- `--:--` decision: ship as a **vertical slice** — foundation (tokens + light/dark + fonts/icons + style library) → shell (rail + top bar + `NavItem` nav refactor + privacy blur + ⌘K palette) → one full reference screen (**Overview** = Dashboard remapped to real PLN data). De-risks the whole program before the mechanical per-screen migration in Sprints 29+.
- `--:--` finding: current state is the opposite of the target — `App.axaml` is **pinned to Light** and every one of ~20 `.axaml` views hardcodes light-only hex; no theming, no palette, no privacy blur; the shell is a 240px text sidebar with 12 bool-driven nav commands. Theme persistence will mirror `FileLanguageStore` (`theme.json`, pre-login readable); nav refactors to a data-driven `NavItem` list feeding both the rail and the palette.
- `--:--` plan written (`sprint-28.md`), Status: Planned, three PRs (28-A foundation → 28-B shell → 28-C Overview) + a Sprints 29–32 redesign roadmap. Plan PR #214 merged.
- `--:--` decision (owner, open-question resolved): **default theme stays Light until the migration completes** (Sprints 29–31), then flip the default to Dark. Dark is fully correct on migrated surfaces (shell + Overview); the not-yet-migrated light-hardcoded views stay readable in Light. `FileThemeStore` therefore defaults to **Light** for now (not Dark as the plan's first draft assumed); the toggle + persistence still ship. Supersedes the plan's "dark default" until program end.
- `--:--` decision (owner, open-question resolved): **Phosphor icons ship as an embedded ttf** (`avares://` asset) referenced through a name→glyph helper (`ph` regular / `ph-fill`), matching the prototype's web-font usage — not per-icon SVG paths.
- `--:--` 28-A complete (steps 28.1–28.6). Shipped: `Theme/Tokens.axaml` (light/dark `ThemeDictionaries` — full palette as Color+Brush + 8 category colour pairs; tiles use ~16% alpha over the panel so no precomputed tint is needed); `IThemeStore`/`FileThemeStore` (`theme.json`, Light default) + DI + startup `ApplySavedTheme` + `ThemeManager` (AppTheme↔ThemeVariant apply); embedded **Hanken Grotesk** (OFL) + **Phosphor**/**Phosphor-Fill** (MIT) fonts under `Assets/Fonts/` (with licences) via `<AvaloniaResource Include="Assets/**"/>`; `Theme/Fonts.axaml` family resources; a `PhosphorIcon` markup extension (name→glyph, 39 icons, throws on unknown); `Styles/Components.axaml` (panel, label/title/muted, money +pos/neg/hidden-blur, KPI figure, icon tile, rail/top-bar/accent buttons, chips, progress bar +over); `App.axaml` un-pinned + merges tokens/fonts/styles + default Hanken font on every Window.
- `--:--` validation: built a dev-only `StyleGalleryWindow` (guarded by the `COFFER_STYLE_GALLERY` env var — no normal-flow impact) exercising every token + component; captured it in **both** Light and Dark via `PrintWindow` — fonts, Phosphor icons, category tints, chips, rail-active fill, progress zones, money pos/neg, and the privacy blur all render correctly per theme. 695 tests green (45 Core + 193 Application + 457 Infrastructure — +4 `FileThemeStore`); `dotnet format` clean on changed files (only pre-existing unrelated CRLF noise remains).
- `--:--` 28-B complete (steps 28.7–28.12). Nav refactored: `MainViewModel` drops the 12 bool props + `Show*` commands for a data-driven `NavItem` list (key/localized-title/Phosphor-icon/section-VM/load/`IsActive`) + a single `NavigateCommand`, `ActiveTitle`, `HideBalances`, `ToggleBalancesCommand`, `ToggleThemeCommand` (via `IThemeSwitcher`), `IsDarkTheme`, and a `CommandPaletteViewModel` opened by `OpenCommandPaletteCommand`; titles + version re-resolve on `LanguageChanged`. `MainWindow` rewritten to the terminal chrome — 60px icon rail (logo mark, `NavItems` ItemsControl with active=filled+ActiveBg, gear pinned bottom) + 52px top bar (title, search/⌘K affordance, theme sun/moon, privacy eye, accent New) + content host; window 1400×880 (min 1180). ⌘K palette overlay (dim backdrop, autofocus, ↑/↓/↵/esc, backdrop-close, tap-to-run) driven by `CommandPaletteViewModel`. New: `IThemeSwitcher`/`ThemeSwitcher` (persist via `IThemeStore`), `PhosphorGlyphConverter` + `ShellIconConverters`, `NavItem`, `PaletteCommand`. `Palette.*`/`TopBar.*` resx keys (parity 455). Tests: rewrote `MainViewModelTests` (nav/title/flags/toggles/palette) + new `CommandPaletteViewModelTests` (filter/wrap/execute) → 700 green (45 + 198 + 457); format clean on changed files.
- `--:--` decision/limitation: the post-login shell can't be screenshot headlessly (the real vault's DPAPI cache is expired and the master password is the owner's), so 28-B is verified by the green build + full nav/palette unit tests + a startup smoke run (the app boots cleanly under the `App`/`MainWindow` rewrite — the login window renders in the new Hanken font, no crash). Visual QA of the assembled shell is a manual step for the owner (log in) or lands with 28-C once a token-consuming screen exists to view. Component styles themselves are already visually verified via the 28-A gallery.
